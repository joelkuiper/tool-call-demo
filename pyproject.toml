[project]
name = "tool-call-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai",
]

[dependency-groups]
dev = [
    "ipython>=9.8.0",
]
